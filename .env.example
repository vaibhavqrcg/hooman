# Optional: SQLite DB URL for Prisma (default: workspace/hooman.db at project root).
# DATABASE_URL=file:./workspace/hooman.db

# Optional: API port (default 3000).
# PORT=3000

# Required: Redis for event queue and kill switch. API and listeners only push events; the event-queue worker processes them.
# Example: redis://localhost:6379 (docker compose up -d redis).
# REDIS_URL=redis://localhost:6379

# Production build: set when building the web app so it can call the API (e.g. http://localhost:3000).
# VITE_API_BASE=http://localhost:3000

# Optional: working directory for stdio MCP and filesystem server (default: workspace/mcpcwd).
# MCP_STDIO_DEFAULT_CWD=./workspace/mcpcwd

# Optional: MCP Server Manager timeouts (when MCP_USE_SERVER_MANAGER is enabled in Settings). Connect: max ms to build session (default 300000 = 5 min). Close: max ms to close session (default 10000).
# MCP_CONNECT_TIMEOUT_MS=300000
# MCP_CLOSE_TIMEOUT_MS=10000

# Optional: override project root for skills (default: repo root). Skills are in <project>/.agents/skills.
# SKILLS_CWD=/path/to/project

# Optional: API base URL (e.g. for capabilities; default http://localhost:3000).
# API_BASE_URL=http://localhost:3000

# Optional: Chrome/Chromium path for WhatsApp (whatsapp-web.js). On macOS we default to Google Chrome; set this if you use another browser or path.
# PUPPETEER_EXECUTABLE_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome

# Optional: Web UI login. When all three are set, the API is reachable from any host and requires login (JWT).
# Username (plain). Use yarn hash-password to generate WEB_AUTH_PASSWORD_HASH from your password.
# WEB_AUTH_USERNAME=admin
# WEB_AUTH_PASSWORD_HASH=$argon2id$v=19$m=65536,t=3,p=4$...
# JWT_SECRET=your-strong-secret-at-least-32-chars

# Required: ngrok authentication token. Get it from https://dashboard.ngrok.com/get-started/your-authtoken.
# NGROK_AUTHTOKEN=your-authtoken
